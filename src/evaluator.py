# Perplexity (language modeling)
# Model size on disk (MB/GB) → how much memory saved.
#
# VRAM usage during inference → critical for deployment.
#
# Inference latency / throughput (tokens/sec).
#
# Training time (wall-clock per epoch or per batch).
#
# Quantization impact (accuracy drop vs. memory saved).